services:
  web:
    image: ${DOCKER_IMAGE_TAG}
    container_name: gentsity-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
